# W23-03 课表编排能看见

## 今晚能感觉到什么

打开 `/path/` 能看见八层由浅入深：基础 → 内核 → 类型 → 底层 → 数据 → 框架 → 工程 → 观测。点进一周能读七段课文、开源讲解和架构图。不是再开一个文档站。

## 和 Java 不同的一句

Java：再做一个 docs 微服务。  
Django：课表 YAML + 本仓 md，同一套模板渲染。

## 示范（先原样跑）

```powershell
uv run python src/forge_web/manage.py test forge_web.tests.test_course_path
```

## 反例（必错）

```text
把别人的书全文贴进仓，或新开 Docusaurus——禁止
```

## 今晚只改这一刀

- `courses/modules.yaml` 八层编排
- `/path/` 路径图 + `/lessons/Wxx/` 课文阅读
- 官方文档链接（CPython / Django / packaging），自写短讲解
- mermaid 架构图 / 流程图

禁止：独立文档仓、SPA、抄开源全书。

## 验收命令

```powershell
uv run python src/forge_web/manage.py test forge_web.tests.test_course_path
```

## 下一课怎么接

编排能看。之后只往本层补讲解和本课 stub，不新开第二产品。
