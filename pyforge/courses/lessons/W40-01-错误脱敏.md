# W40-01 错误脱敏

## 今晚能感觉到什么

`redact_event` 把 `password` / `token` 打成 `***`。测试断言原文不在结果里。

## 和 Java 不同的一句

Java：日志脱敏过滤器。  
Python：自己写纯函数，先测后挂。

## 示范（先原样跑）

```powershell
uv run pytest tests/test_error_redact.py -q
```

这是 **G10**。

## 反例（必错）

```python
log.info("login %s", payload)  # payload 里有密码
```

## 今晚只改这一刀

- `src/pyforge/services/redact.py`
- `tests/test_error_redact.py`

禁止：SaaS APM 当作业。

## 验收命令

```powershell
uv run pytest tests/test_log_context.py tests/test_error_redact.py -q
uv run python src/forge_web/manage.py test forge_web.tests.test_metrics
```

## 下一课怎么接

脱敏绿了。下一课用 YAML DSL 吃 `courses/curriculum.yaml`。
