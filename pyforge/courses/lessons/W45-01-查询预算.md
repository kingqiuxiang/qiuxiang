# W45-01 查询预算

## 今晚能感觉到什么

周列表测试断言 SQL 条数 ≤ 8。不要微服务拆库。

## 和 Java 不同的一句

Java：Hibernate 统计。  
Django：`assertNumQueries`。

## 示范（先原样跑）

```powershell
uv run python src/forge_web/manage.py test forge_web.tests.test_query_budget
```

## 反例（必错）

```python
for week in weeks:
    week.sessions.all()  # N+1
```

## 今晚只改这一刀

- 预取
- `test_query_budget`

禁止：微服务。

## 验收命令

```powershell
uv run python src/forge_web/manage.py test forge_web.tests.test_query_budget
```

## 下一课怎么接

查询收敛了。下一课 prod doctor：`DEBUG=True` 在 prod 失败。
