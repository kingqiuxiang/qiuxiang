# W20-01 周报聚合

## 今晚能感觉到什么

同一周两条 session，周报 `session_count == 2`，tags 去重。没有图表库。

## 和 Java 不同的一句

Java：Stream `groupingBy`。  
Python：自己聚。不上 matplotlib。

## 示范（先原样跑）

```powershell
uv run pytest tests/test_weekly_report.py -q
```

这是 **G5**。不过，不准建 Django。

## 反例（必错）

```python
# 为了好看 pip install matplotlib——今晚禁止
```

## 今晚只改这一刀

- `src/pyforge/services/weekly_report.py`
- `tests/test_weekly_report.py`

禁止：Django / frontend。

## 验收命令

```powershell
uv run pytest tests/test_import_events.py tests/test_weekly_report.py tests/test_gate_attempts.py -q
```

## 下一课怎么接

G5 过了才准建 Django。下一课对照 Spring：`/health/` 返回 ok。
