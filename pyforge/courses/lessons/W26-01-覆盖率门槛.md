# W26-01 覆盖率门槛

## 今晚能感觉到什么

`uv run pytest --cov=src/pyforge --cov-fail-under=80 -q` 绿。不到 80 就红。

## 和 Java 不同的一句

Java：JaCoCo 门槛。  
Python：pytest-cov。不要压测。

## 示范（先原样跑）

```powershell
uv run pytest --cov=src/pyforge --cov-fail-under=80 -q
```

## 反例（必错）

```text
为了数字去写空测试骗行覆盖——看见就打回
```

## 今晚只改这一刀

- 补齐内核测试直到 80
- 命令进文档

禁止：压测。

## 验收命令

```powershell
uv run pytest --cov=src/pyforge --cov-fail-under=80 -q
```

## 下一课怎么接

覆盖率有了。下一课 CLI 和 Web 对同一条 session 对账。
