# W36-01 番茄 + 错题

## 今晚能感觉到什么

`manage.py pyforge_plugins` 列出 `pomodoro` 和 `wrongbook`。配置里 disable 一个，列表不再启用它。不准做第三个。

## 和 Java 不同的一句

Java：Spring `@ConditionalOnProperty`。  
Django：管理命令读配置。

## 示范（先原样跑）

```powershell
uv run python src/forge_web/manage.py pyforge_plugins
```

这是 **G9** 的 Web 面；测试仍要 `test_plugin_isolation` + `test_plugin_discover`。

## 反例（必错）

```text
再写一个“AI 教练”第三插件——禁止
```

## 今晚只改这一刀

- 两个插件 + disable
- 管理命令

## 验收命令

```powershell
uv run python src/forge_web/manage.py pyforge_plugins
```

## 下一课怎么接

插件面有了。下一课 structlog 给请求挂 `request_id`。
