# W42-01 门禁引擎

## 今晚能感觉到什么

跑一个会失败的 gate，`gate_attempts` 多一行，带着失败原因。不是分布式 cron。

## 和 Java 不同的一句

Java：批作业框架。  
Python：复用 W14 GateRunner，写回 W18 表。

## 示范（先原样跑）

```powershell
uv run pytest tests/test_gate_engine.py -q
```

## 反例（必错）

```text
上 Celery beat——G6 之前本就禁止，现在仍不要分布式 cron
```

## 今晚只改这一刀

- `src/pyforge/services/gate_engine.py`
- `tests/test_gate_engine.py`

## 验收命令

```powershell
uv run pytest tests/test_gate_engine.py -q
```

## 下一课怎么接

记录有了。下一课 HTMX 筛选周列表，只刷新 tbody。
